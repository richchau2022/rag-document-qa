# --- Core LangChain (1.x line) ---
langchain==1.3.11
langchain-community==0.4.2
langchain-text-splitters==1.1.2

# --- Milestone 1: Document Ingestion ---
pypdf==5.1.0          # PDF parser behind PyPDFLoader
tiktoken==0.8.0       # token counting for chunk sizing
python-dotenv==1.0.1  # load API keys from .env

# --- Milestone 2: Embeddings + Vector Storage ---
chromadb==1.5.9            # local persistent vector database
langchain-chroma==1.1.0    # LangChain <-> Chroma integration
langchain-voyageai==0.4.0  # Voyage AI embeddings (Anthropic-recommended)

# Later milestones will add: anthropic, fastapi, uvicorn, ragas, deepeval
