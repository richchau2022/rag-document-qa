# --- Milestone 1: Document Ingestion ---
# LangChain core + the community package where document loaders live.
langchain==0.3.27
langchain-community==0.3.27
# pypdf is the actual PDF parser that PyPDFLoader wraps.
pypdf==5.1.0
# Token-aware text splitting needs a tokenizer; tiktoken gives us real token counts.
tiktoken==0.8.0
# Loads secrets (API keys) from a .env file into environment variables.
python-dotenv==1.0.1

# Later milestones will add: chromadb, anthropic, fastapi, uvicorn, ragas, deepeval
